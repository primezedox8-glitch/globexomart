# Optional upstream proxies used by the server.
# Leave empty for direct connection.
# Supports http://, https://, socks5:// and socks:// URLs.
# Multiple proxies can be separated by comma, semicolon or newline.
# Example format only; fill your own proxies:
# UPSTREAM_PROXY_LIST=socks5://user:pass@127.0.0.1:1080,http://user:pass@proxy.example.com:8080
UPSTREAM_PROXY_LIST=

# Single proxy fallback. Used after UPSTREAM_PROXY_LIST / UPI_PROXY_LIST if provided.
UPSTREAM_PROXY=

# Backward-compatible alias; also supported by the extractor.
UPI_PROXY_LIST=

# Max number of proxy/direct attempts for one extraction request.
# Empty means try all configured proxies, or direct once if no proxy is configured.
MAX_PROXY_ATTEMPTS=

# ChatGPT account check timezone offset. Default: -480.
CHATGPT_TIMEZONE_OFFSET_MIN=-480
